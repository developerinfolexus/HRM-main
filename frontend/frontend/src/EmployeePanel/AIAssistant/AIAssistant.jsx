import React from 'react';
import ChatLayout from '../../components/Chat/ChatLayout';

/**
 * Reuses the main ChatLayout component for the AI Assistant view,
 * ensuring a consistent UI/UX across the application ("Full Chat Code").
 */
const AIAssistant = () => {
    return (
        <ChatLayout />
    );
};

export default AIAssistant;
